<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.matias.firebase.HomeFragment"
        android:label="HomeFragment" >
        <action
            android:id="@+id/action_homeFragment_to_documentFragment"
            app:destination="@id/documentFragment" />
    </fragment>
    <fragment
        android:id="@+id/documentFragment"
        android:name="com.matias.firebase.DocumentFragment"
        android:label="DocumentFragment" >
        <action
            android:id="@+id/action_documentFragment_to_homeFragment"
            app:destination="@id/homeFragment" />
    </fragment>
</navigation>
